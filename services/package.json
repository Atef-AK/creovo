{
    "name": "creovo-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "python -m uvicorn lensio.api.app:app --reload --host 0.0.0.0 --port 8000",
        "start": "python -m uvicorn lensio.api.app:app --host 0.0.0.0 --port 8000",
        "lint": "ruff check .",
        "typecheck": "mypy ."
    }
}